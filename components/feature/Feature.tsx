function Feature({ num }) {
  return (
    <div className="bg-slate-500 hover:bg-slate-600 p-4 rounded hover:scale-110 transition-all duration-200">
      Feature: {num}
    </div>
  );
}

export default Feature;
